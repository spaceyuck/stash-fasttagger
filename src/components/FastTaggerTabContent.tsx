const { React } = window.PluginApi;

const FastTaggerTabContent = (props: FastTaggerTabContentProps) => {
  return (
    <div>tagger view goes here</div>
  );
};

export default FastTaggerTabContent;

interface FastTaggerTabContentProps {
}