const { React } = window.PluginApi;

const FastTaggerContent = (props: FastTaggerContentProps) => {
  return (
    <div>tagger view goes here</div>
  );
};

export default FastTaggerContent;

interface FastTaggerContentProps {
}